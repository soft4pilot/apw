// APW 1.1
// Modelo/Empresa (Acciones)

import types from "./types";

// Obtener metadatos de la empresa
const getCompanyMetadata = () => ({
    type: types.GET,
});


export default {
    getCompanyMetadata,
};
